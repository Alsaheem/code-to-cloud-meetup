name: iam
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
description: this project stores everything relating to iam and user permissions
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
